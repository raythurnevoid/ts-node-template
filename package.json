{
	"name": "@raythurnevoid/ts-node-template",
	"version": "0.1.0",
	"description": "Template for TS Node projects",
	"scripts": {
		"clean-lib": "del-cli lib",
		"copy": "copyfiles src/**/*.* package.json tsconfig.json tsconfig.lib.json lib",
		"tsc": "tsc -p tsconfig.lib.json",
		"lib": "npm-run-all clean-lib copy tsc",
		"prepackage": "npm run lib",
		"package": "npm publish ./lib --access public",
		"env": "cross-env TS_NODE_PROJECT=\"tsconfig.json\"",
		"ts-node": "npm run env -- node -r ts-node/register -r tsconfig-paths/register --loader ts-node/esm --",
		"ts-node:inspect": "npm run ts-node -- --inspect --",
		"start": "npm run ts-node src/index.ts",
		"debug": "npm run ts-node:inspect src/index.ts",
		"test": "jest"
	},
	"type": "module",
	"browserslist": [
		"> 5% or node >= 16"
	],
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/raythurnevoid/ts-node-template.git"
	},
	"author": "Ray Thurne <raythurnevoid@gmail.com>",
	"license": "ISC",
	"dependencies": {
		"@raythurnevoid/node-colorful-log": "^0.2.5",
		"copyfiles": "^2.4.1",
		"cross-env": "^7.0.3",
		"del-cli": "^4.0.0",
		"dotenv": "^10.0.0",
		"npm-run-all": "^4.1.5",
		"ts-node": "^10.0.0",
		"tsconfig-paths": "^3.9.0",
		"typescript": "^4.3.2"
	},
	"devDependencies": {
		"@types/node": "^15.12.2",
		"browserslist": "^4.16.6"
	}
}
